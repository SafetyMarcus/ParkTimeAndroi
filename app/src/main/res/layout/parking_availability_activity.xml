<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <include layout="@layout/toolbar"/>

    <TextView
        android:id="@+id/location"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:textSize="20sp"
        android:transitionName="@string/location_transition"
        tools:text="123 Fake Street"
        android:gravity="center"/>

    <Button
        android:id="@+id/parking_available"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:transitionName="@string/parking_available_transition"
        android:text="Yes"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:background="@drawable/green_selector"/>

    <Button
        android:id="@+id/parking_unavailable"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:transitionName="@string/parking_unavailable_transition"
        android:text="No"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:background="@drawable/red_selector"/>
</LinearLayout>